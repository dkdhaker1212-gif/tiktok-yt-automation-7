name: "Channel 7 - Daily Upload (17:00 UTC / 1 PM ET)"

on:
  schedule:
    - cron: "0 17 * * *"      # 17:00 UTC daily = 1:00 PM US Eastern; 10:30 PM IST
  workflow_dispatch:
    inputs:
      channel:
        description: "channel id"
        required: false
        default: "channel_7"
      dry_run:
        description: "true = list+pick only, never upload"
        required: false
        default: "false"
      force:
        description: "true = ignore the per-day guard (post a second one today)"
        required: false
        default: "false"

# The per-day guard (runs table) makes re-triggers on the same UTC day a no-op,
# so a delayed cron run + a manual dispatch cannot double-post.

concurrency:
  group: upload-channel_7
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  upload:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure ffmpeg
        run: |
          if command -v ffmpeg >/dev/null 2>&1 && command -v ffprobe >/dev/null 2>&1; then
            echo "ffmpeg already present"; ffmpeg -version | head -1; exit 0
          fi
          echo "installing ffmpeg via apt"
          timeout 180 sudo apt-get update -y || true
          timeout 300 sudo apt-get install -y ffmpeg || true
          if command -v ffmpeg >/dev/null 2>&1; then exit 0; fi
          echo "apt failed; pulling a static build"
          timeout 300 curl -fL --retry 3 -o /tmp/ff.tar.xz \
            https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
          mkdir -p /tmp/ff && tar -xJf /tmp/ff.tar.xz -C /tmp/ff --strip-components=1
          sudo install /tmp/ff/ffmpeg /tmp/ff/ffprobe /usr/local/bin/
          ffmpeg -version | head -1

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Restore secrets to files
        env:
          CH_CLIENT_SECRET: ${{ secrets.CHANNEL_7_CLIENT_SECRET }}
          CH_TOKEN: ${{ secrets.CHANNEL_7_TOKEN }}
          TIKTOK_COOKIES: ${{ secrets.TIKTOK_COOKIES }}
        run: |
          mkdir -p credentials tokens
          printf '%s' "$CH_CLIENT_SECRET" | base64 -d > credentials/channel_7_client_secret.json
          printf '%s' "$CH_TOKEN"         | base64 -d > tokens/channel_7_token.json
          if [ -n "$TIKTOK_COOKIES" ]; then
            printf '%s' "$TIKTOK_COOKIES" | base64 -d > cookies.txt
            echo "TIKTOK_COOKIES_FILE=$PWD/cookies.txt" >> "$GITHUB_ENV"
          fi
          python -c "import json;json.load(open('credentials/channel_7_client_secret.json'));json.load(open('tokens/channel_7_token.json'));print('secret files OK')"

      - name: Write .env
        run: |
          echo "DISCORD_WEBHOOK_URL=${{ secrets.DISCORD_WEBHOOK_URL }}" > .env
          echo "ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}" >> .env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "DRY_RUN=${{ github.event.inputs.dry_run }}" >> .env

      - name: Run slot 1
        run: |
          ARGS="--slot 1 --channel ${{ github.event.inputs.channel || 'channel_7' }}"
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          if [ "${{ github.event.inputs.force }}" = "true" ]; then ARGS="$ARGS --force"; fi
          python run.py $ARGS

      - name: Commit DB
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          python -c "import sqlite3;c=sqlite3.connect('data/channel_7.db');c.execute('PRAGMA wal_checkpoint(TRUNCATE)');c.close()"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/channel_7.db
          git commit -m "state: slot 1 run $(date -u +%FT%TZ)" || { echo "nothing to commit"; exit 0; }
          for i in 1 2 3; do
            git fetch origin main && git rebase origin/main && git push origin HEAD:main && exit 0
            echo "push attempt $i failed; retrying"; sleep 5
          done
          echo "could not push DB after 3 tries"; exit 1

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: logs-slot1-${{ github.run_id }}
          path: logs/
          retention-days: 14
          if-no-files-found: ignore
